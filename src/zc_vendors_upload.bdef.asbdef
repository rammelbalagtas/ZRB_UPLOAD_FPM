projection;
strict ( 2 );
use draft;
use side effects;

define behavior for ZC_VENDORS_UPLOAD //alias <alias_name>
{
  use action uploadFile1;
  use action uploadFile2;
  use action processAll;
  use action deletePreviousRecords;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}